import styled from 'styled-components'

export const MainContainer = styled.div`
    background-image: linear-gradient(${props => props.first});
    padding: 20px, 10px, 20px, 10px;
    color:#ffffff;
`
